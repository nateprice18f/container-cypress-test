{
  "name": "Cypress Container",
  "image": "<Container name>",
  "mounts":[
    {
      "source": "${localWorkspaceFolder}", 
      "target": "/workspace",
      "type": "bind",
      "consistency": "consistent",
      "readonly": true
    }
  ]
}
